import { RefreshCcw, createElement } from "lucide";

const RefreshSvg = createElement(RefreshCcw);
RefreshSvg.setAttribute("width", "100");
RefreshSvg.setAttribute("height", "100");

export const RefreshIcon = RefreshSvg.outerHTML;
